const User = require('../models/userModel')

const register = async(req, res, next) => {
    try {
        
    } catch (error) {
        
    }
}

const login = async(req, res, next) => {
    try {
        
    } catch (error) {
        
    }
}

module.exports = {
    register,
    login
}